<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itwill.semiproject.repository.ReservationAreaDao">
    
    <select id="selectReservedArea" resultType="ReservationArea">
        select 
           area_id
        from 
            reservation_area
        where area_id not in (
            select area_id
            from reservation_master
            where res_check_in = TO_DATE(#{resCheckIn}, 'YYYY-MM-DD')
        )
        order by area_id;
    </select>

</mapper>