<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itwill.semiproject.repository.ReservationDetail">
    
     <!-- 예약 상세 정보 삽입 -->
    <insert id="insert" parameterType="ReservationDetail">
        INSERT INTO reservation_detail (item_id, item_quantity, item_totalprice) VALUES (#{itemId}, #{itemQuantity}, #{itemTotalPrice})
    </insert>

</mapper>