<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itwill.semiproject.repository.PaymentCancelDao">

    <!-- 취소 테이블 insert -->
    <insert id="insertPaymentCancel">
        INSERT INTO PAYMENTCANCEL 
        (can_price, can_app_date, can_wit_date, can_role, can_cs_status)
        VALUES (#{canPrice}, #{canAppDate}, #{canWitDate, jdbcType=DATE}, #{canRole}, #{canCsStatus})
    </insert>

</mapper>